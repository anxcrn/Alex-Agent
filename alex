#!/usr/bin/env python3
"""
Alex Agent CLI launcher.

This wrapper should behave like the installed `alex` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from alex_cli.main import main
    main()
